# Database Configuration
# Priority: DATABASE_URL takes precedence over individual POSTGRES_* variables
# For Render deployment: Set DATABASE_URL to the full connection string from Render PostgreSQL
# For local development: Set POSTGRES_* variables or use DATABASE_URL

POSTGRES_DB=inventory_db_nuki
POSTGRES_USER=inventory_user
POSTGRES_PASSWORD=<RENDER_DB_PASSWORD>
POSTGRES_HOST=dpg-d8g0813eo5us73fuf2hg-a
POSTGRES_PORT=5432
DATABASE_URL=<RENDER_DATABASE_URL>

# Application Configuration
APP_NAME=Inventory & Order Management System API
APP_VERSION=1.0.0
ENVIRONMENT=development
LOG_LEVEL=INFO

# Frontend Configuration (for Vite build-time)
VITE_API_BASE_URL=http://localhost:8000/api/v1
